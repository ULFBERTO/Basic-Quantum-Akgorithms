{
    "buildCommand": "cd QuantumDocs && npm install && npm run build",
    "outputDirectory": "QuantumDocs/dist/quantum-docs/browser",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}